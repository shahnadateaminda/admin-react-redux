import React from "react";

export default function Dashboard(props) {

    return (
        <div>
            <main >
                <div />
                {props.children}
            </main>
        </div>

    );
}
